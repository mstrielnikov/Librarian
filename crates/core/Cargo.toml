[package]
name = "graphodoc_core"
version = "0.1.0"
edition = "2024"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
# FIX: Add xxhash-rust for graph ID generation
xxhash-rust = { version = "0.8", features = ["xxh3"] }
